buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.2.3'
    }
}

ext {
    compileSdkVersion = 19
    buildToolsVersion = "20.0.0"
}

allprojects {
    repositories {
        mavenCentral()
    }
}